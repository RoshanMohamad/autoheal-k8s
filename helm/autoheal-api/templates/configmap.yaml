apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "autoheal-api.fullname" . }}-config
  labels:
    {{- include "autoheal-api.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
